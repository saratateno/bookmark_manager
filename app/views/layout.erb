<!DOCTYPE html>
<html>

  <head>
    <title>Book Mark Manager</title>
  </head>

  <body>
    <% if current_user %>
      Welcome <%= current_user.email %>
    <% end %>

    <%= yield %>
  </body>
</html>
