<!DOCTYPE html>
<html>
  <h1> Links </h1>

  <a href="/links/new" >Add Bookmark</a>


  <ul id='links'>
    <% @links.each do |link| %>
      <li>
        Title: <%= link.title %>
        URL:   <%= link.url %>
        <% tags_to_display = link.tags.map(&:name) %>
        Tags: <%= tags_to_display.join(', ') %>
      </li>
      <% end %>
  </ul>

</html>
